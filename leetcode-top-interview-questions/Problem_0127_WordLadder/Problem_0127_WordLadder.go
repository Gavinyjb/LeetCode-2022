package Problem_0127_WordLadder

func ladderLength(beginWord string, endWord string, wordList []string) int {
	wordId := make(map[string]int)
	graph := make([][]int, 0)
	addWord := func(word string) int {

	}
}
